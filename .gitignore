root.log
.idea/
dataset/**
!dataset/.gitignore
# uv_wavelengths/sma.fits is REAL SMA uv baseline coverage — a read-only input to the
# interferometer simulators, with no writer anywhere. It is NOT regenerable; never purge it.
!dataset/interferometer/uv_wavelengths/**
output/
test_report.md
test_results/
__pycache__/
*.pyc
*.nbc
*.nbi

# Generated artifacts — never check in
image.fits
path/
scripts/path/
scripts/scripts/
output_path/
*.log
.codex/

# Structured smoke/validation reports (run_smoke.py --report-dir)
test-results/
